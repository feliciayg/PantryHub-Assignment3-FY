<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.InventoryHub.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_md"
    app:cardBackgroundColor="@color/inventory_surface_muted"
    app:strokeColor="@color/inventory_surface_muted">

    <LinearLayout
        android:id="@+id/topicContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:orientation="vertical"
        android:padding="@dimen/card_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/titleTextView"
                style="@style/TextAppearance.InventoryHub.SectionTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:maxLines="2"
                android:textColor="@color/inventory_text_secondary"
                tools:text="Getting Started" />

            <ImageView
                android:id="@+id/chevronImageView"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:contentDescription="@null"
                android:rotation="0"
                android:src="@drawable/ic_chevron_down"
                app:tint="@color/inventory_text_secondary" />
        </LinearLayout>

        <TextView
            android:id="@+id/answerTextView"
            style="@style/TextAppearance.InventoryHub.Body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_md"
            android:lineSpacingExtra="2dp"
            android:textColor="@color/inventory_text_secondary"
            android:visibility="gone"
            tools:text="Start by adding locations, items, and your first purchase."
            tools:visibility="visible" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
